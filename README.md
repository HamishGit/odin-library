# odin-library

A site that displays a list of books, each with a title, author, number of pages, and a status of whether or not it has been read.

## Functionality
- Add new books via a modal window.
- Delete books via a button on each book.
- Toggle the 'has been read' property on each book via a button.
